# 了解如何使用Solidity
## 学习solidity推荐教程
完整应用: https://github.com/RemoteCodeCamp/solidity-v2
# Solidity 教学大纲

## 1. Solidity 简介
   - Solidity 是什么？
   ethereum合约的编程语言
   - Solidity 的特点和用途
   Solidity 是图灵完备语言，这意味着可以通过明确的定义和编码编写复杂的合同
   
## 2. Solidity 安装与使用
   - 安装 Remix IDE
   - Remix IDE 界面简介
   
## 3. Hello World
   - 编写简单的 Solidity 合约
   1.编写合约sol文件
   2.编译合约文件
   3.部署合约
   4.回调内部事件
   - 在 Remix IDE 中编译和部署合约
   
## 4. Solidity 合约组成结构
   - 合约结构概述
   - 合约的基本组成部分
   1.每个智能合约将分配有一个 20 个字节的地址，用作合约的唯一标识。
   2. 有一些状态和事件机制
    
## 5. 注释
   - Solidity 中的注释类型和用法
   
   
## 6. 数据类型
   - Solidity 中的基本数据类型
   引用类型和布尔类型
   
## 7. 变量
   - 定义和声明变量
   变量的作用域。Authority 里面是有三种变量的。合约内。函数 Y 的变量称为状态变量。比如我们之前示例里面的 hello。然后在函数内的是局部变量。其实还有第三种全局变量是在合 U 外的是由 solid。内置的。是不能够创建的变量，称为全局变量。然后状态变量的数据是？是存储在链上的。但是gas 的消耗是比较高。然后局部变量？然后是一般是存储到内存 memory 里面的，然后盖子消耗比较低。

   - 变量的作用域和生命周期
   
常量和不可变量。然后 cost 的常量是？修饰的变量是需要在编译期确定值的。就比如我例子里面，下面例子里面的 U256。很 constant 修饰的？这刚才那。事实。然后 constant。是哈勒索液体？然后 constant address。地址？当你去声明一个常量的时候。他在。编译的时候就已经确定了值。在其使用常量的地方是把。只是替换过去的。immutable 的话是不可变量。它的变量在部署的时候确定变量的值。它其实是可以在。构造函数里面。
说话人 23:20
去进行复制，但是只能进行一次复制。之后就不能再变了。这种方式。导致 immutable 的是非常灵活的。在常见的 E rc20合约中。用来指示小数位置的。它是不能修改的变量。很多时候是需要在创建合约的时候去制定它的值。这里也有一个例子。用 im table 的 numb？设置成一个值。构造函数里面构造函数后面会介绍。可以给它进行修改。但是。一旦出了。构造函数作用域之后值就确定下来了。然后还有 emit address？里面有 block。他们都是在构造方法里面进行初始化的。这种方式是非常灵活的。也是里面一个？非常有趣的。变量类型。

   
## 8. 数据位置
   - 内存、存储和栈上数据的区别和使用
   
## 9. 布尔类型
   - Solidity 中的布尔类型及其用法
   
## 10. 整型
   - 不同类型的整数及其范围
   int8到int256
   unit8到unit256
   
## 11. 地址类型
   - 地址类型的特点和用法
   address是定长20的字节数组
   address 是有特殊的成员变量
   address public _address = 0x23224234234234sdfs4353sdfsf
   
## 12. 静态字节数组
   - 定义和使用静态字节数组
   bytes32 public _bytes32 = "Minisolidityt"

   
## 13. 字面值
   - Solidity 中常见的字面值表示
   
## 14. 枚举类型
   - 定义和使用枚举类型
   enum
   enum 可以显示的和unitN相互转换
   
## 15. 自定义值类型
   - 如何定义和使用自定义值类型
   
## 16. 数组
定长数组
unit[2] memory x = [1, 2]
unit[] memory x = [1, 2, 3, 4]
变长数组

   - 数组的定义和初始化
   - 数组的操作和常见用法
   
## 17. 数组切片
   - 对数组的部分进行操作和访问
   
## 18. 数组成员
   - 数组的成员属性和方法
   
## 19. 多维数组
   - 定义和操作多维数组
   
## 20. 动态字节数组
   - 动态字节数组的定义和使用
   
## 21. 结构体
   - 定义和使用结构体
   struct Student {
      unit256 id,
      unit256 source
   }

   
## 22. 映射类型
   - 定义和使用映射
   
## 23. 控制结构
   - if-else 语句
   - for 循环
   - while 循环
   - do while 循环
   - break 和 continue 语句
   
## 24. require、assert 和 revert
   - 区别和用法
   
## 25. 合约及其组成结构
   - 合约概述
   - 合约的基本组成部分
   
## 26. constant 和 immutable
   - 区别和用法
   
## 27. 函数
   - 函数的定义和调用
   分为内部函数调用和外部函数调用

   函数可见性还有四种 internal 的话是表示只有内部。只有从合约内部可以访问。继承的和也可以使用。然后是只能从合约外部来访问。不过用 external 修饰的合约可以用 this。的方式去调用，但是。它就会涉及到。EV 里面EVM 里面的方法调用了，也会在这个会在后面。所以进阶的时候去详细介绍这一部分。大家只要知道修饰的就不能在合约内部去调用。public 的话？内部和外部均可见。private 的话是只能在本合约内部访问继承的合约也不能使用。主要是这。可见性主要是这四种

   - 函数的参数和返回值
   可以返回多个，也可以返回单个
   
## 28. 构建函数
   - 合约的构建函数定义和使用
   constructor。哪个合约是只能唯一定义的，在部署合约的时候会自动运行一次，用来初始化合约的一些参数
   
## 29. 修饰器
   - 修饰器的定义和使用
   
## 30. 可见性
   - 函数和状态变量的可见性修饰符
   
## 31. 状态可变性
   - 修改函数状态的关键字
   
## 32. view 和 pure 函数
   - 区别和用法
   
## 33. receive 和 fallback 函数
   - 接收以太币的特殊函数
